class TableCard {

  constructor(user_id, value, description, visibility) {
          this.value = value;
          this.description = description;
          this.user_id = user_id;
          this.visibility = visibility;
  }
}

module.exports = TableCard;
